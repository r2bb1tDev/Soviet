# 🐻 Soviet — Децентрализованный мессенджер

<p align="center">
  <img src="https://raw.githubusercontent.com/r2bb1tDev/Soviet/main/assets/logo/bear_icon.svg" alt="Soviet Logo" width="120"/>
</p>

<p align="center">
  <b>Общайся без серверов, без регистрации, без слежки.</b><br/>
  Работает в локальной сети, через интернет и в P2P mesh-сети.
</p>

<p align="center">
  <a href="https://github.com/r2bb1tDev/Soviet/releases/latest">
    <img src="https://img.shields.io/badge/скачать-v2.6.0-blue" alt="Download"/>
  </a>
  <img src="https://img.shields.io/badge/platforms-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey"/>
  <a href="LICENSE"><img src="https://img.shields.io/badge/license-MIT-green"/></a>
</p>

---

## 📥 Скачать

Перейди на страницу [**Releases**](https://github.com/r2bb1tDev/Soviet/releases/latest) и скачай установщик для своей платформы:

| Платформа | Файл |
|-----------|------|
| 🪟 **Windows** | `Soviet_*_x64-setup.exe` |
| 🍎 **macOS** (Apple Silicon M1/M2/M3) | `Soviet_*_aarch64.dmg` |
| 🍎 **macOS** (Intel) | `Soviet_*_x64.dmg` |
| 🐧 **Linux** (Ubuntu/Debian) | `Soviet_*_amd64.deb` |
| 🐧 **Linux** (другие дистрибутивы) | `Soviet_*.AppImage` |

> Приложение автоматически оповещает о новых обновлениях — просто установи один раз.

---

## 🤔 Что это такое?

**Soviet** — мессенджер без серверов и без регистрации. Никакого email, телефона или аккаунта — только никнейм и пара криптографических ключей.

В отличие от Telegram, WhatsApp или Signal:
- ✅ **Нет центрального сервера** — сообщения идут напрямую между людьми
- ✅ **Нет регистрации** — просто запустил и начал общаться
- ✅ **Нет слежки** — никто не видит твои сообщения, даже разработчики
- ✅ **Работает без интернета** — в одной Wi-Fi сети через LAN-режим
- ✅ **Открытый код** — можешь проверить сам

---

## 💬 Как это работает?

При первом запуске Soviet генерирует твою **пару ключей** (как в Bitcoin):
- **Публичный ключ** — это твой адрес, его можно давать другим
- **Приватный ключ** — хранится на твоём устройстве, никуда не отправляется

Чтобы написать кому-то — нужен его публичный ключ. Поделиться своим можно через QR-код или скопировав приглашение.

### Как передаются сообщения?

Soviet автоматически выбирает лучший способ доставки:

```
1️⃣  LAN (локальная сеть)   — быстро, без интернета, в одной Wi-Fi сети
2️⃣  P2P mesh (интернет)    — напрямую через интернет, без серверов
3️⃣  Nostr relay (fallback) — через публичные relay-серверы, если P2P недоступен
```

Все сообщения зашифрованы — даже relay-серверы не могут их прочитать.

### Офлайн‑сообщения (как в ICQ)

Если собеседник сейчас не в сети, Soviet **сохранит сообщение локально** и **автоматически доставит**, когда контакт появится (LAN/P2P) или через Nostr DM fallback.

---

## ✨ Возможности

### Личные сообщения
- 🔒 End-to-End шифрование (ChaCha20-Poly1305)
- 🗂️ **Вкладки чатов** — несколько чатов открыты одновременно, мгновенное переключение (как в ICQ/IDE)
- ✏️ Редактирование и удаление отправленных сообщений
- 💬 Ответы на сообщения с **jump-to-quote** (клик по цитате → прокрутка к оригиналу с жёлтой вспышкой)
- 😊 Emoji-реакции
- 📎 Файлы и изображения (до 10 МБ) — **drag & drop** + inline-превью картинок (data-URL и ссылки на `.png/.jpg/.webp/.gif`)
- 🎙️ Голосовые сообщения (записываются прямо в приложении)
- 🔔 **Жужжалка (ICQ Nudge)** — разбуди контакта: окно у получателя трясётся + звучит «бззз» через LAN/P2P/Nostr
- 📥 **Экспорт истории** в текстовый `.txt` файл
- 🔎 **Ctrl+F поиск** по истории внутри чата с подсветкой и навигацией
- ✓✓ Статусы доставки (отправлено / доставлено / прочитано) — LAN, P2P и Nostr
- ⌨️ Индикатор "печатает..."
- 📜 Пагинация истории — ленивая загрузка при скролле вверх (по 50 сообщений)

### Группы
- Создание групп до любого размера
- Приглашение по публичному ключу
- Системные уведомления о входе/выходе
- Групповые сообщения работают через LAN, P2P mesh и Nostr

### Каналы (Nostr)
- Публичные каналы через протокол Nostr
- Создание и подписка на каналы по ID
- Только создатель может писать в канал; все могут комментировать
- 🔍 Поиск каналов по названию
- Описание канала отображается в шапке
- ID канала — копируется одним кликом в шапке и в настройках
- Emoji-реакции, ответы, редактирование и удаление постов
- Пересылка сообщений из канала в чат

### Контакты
- Добавление по публичному ключу или QR-коду
- 🔍 **Умный поиск**: `@custom_id`, hex-префикс pubkey, nickname или local_alias — всё одним полем с подсветкой
- Статусы: В сети / Отошёл / Недоступно / Не беспокоить / Невидимка
- 💤 **Auto-away** — 5 мин без активности → статус автоматически «Отошёл»
- 💬 **Готовые сообщения статуса** (6 пресетов: На работе / Обедаю / Занят / В отпуске / На созвоне / Кодю) + свободный ввод
- 🤖 **Away auto-reply** — автоответ при статусе away/dnd с настраиваемым текстом (кулдаун 10 мин на контакта)
- 👁 **Stealth mode** — режим невидимки для отдельных контактов: они не видят нас онлайн
- 🕒 **«Последний раз в сети»** — в профиле контакта
- Аватары и статус-текст

### Поиск
- 🔍 Поиск по всей истории переписки в реальном времени
- Результаты из прямых и групповых чатов, клик открывает нужный чат

### Интерфейс
- 🖤 **Dark-grey Linux-terminal тема** — серые оттенки, DOS-шрифт (Share Tech Mono / VT323), CRT-scanlines
- 🐻 Белый медведь в системном трее
- 🔔 Push-уведомления + **звуковой сигнал** (ICQ-стиль) о новых сообщениях и «жужжалке»
- 🔢 **Счётчик непрочитанных на иконке трея** — цифра обновляется в реальном времени
- 🗂️ **Multi-chat через вкладки** — несколько чатов открыты одновременно
- 📲 QR-код для обмена контактом
- 🔄 Автоматические обновления (подписанные бинари + fallback upload `.sig` в CI)
- ↔️ Изменяемая ширина сайдбара (перетаскивание + сохранение)
- 🔗 Кликабельные ссылки в сообщениях

---

## 🔐 Безопасность

| Компонент | Технология |
|-----------|------------|
| Ключи | Ed25519 (подпись) + X25519 (шифрование) |
| Шифрование сообщений | ChaCha20-Poly1305 |
| Обмен ключами | ECDH X25519 + HKDF |
| Хранение ключей | Системное хранилище ОС (DPAPI / Keychain / libsecret) |
| Шифрование чувствительных полей БД | ChaCha20-Poly1305 + ключ из OS keyring |
| Протокол сети | Noise Protocol (libp2p) |
| NAT traversal | DCUtR (Direct Connection Upgrade through Relay) |

Все прямые сообщения шифруются на твоём устройстве. Ни серверы, ни разработчики не имеют доступа к содержимому. Секретные ключи дополнительно шифруются перед записью в базу данных.

---

## 🌐 Режимы связи

### 1. LAN (без интернета)
Если ты и собеседник в одной Wi-Fi сети — Soviet найдёт друг друга автоматически через mDNS и передаст сообщение напрямую по TCP.

**Подходит для:** офис, дом, мероприятие, закрытая сеть.

### 2. P2P mesh (интернет)
Прямое соединение через интернет без промежуточных серверов. Soviet использует libp2p Kademlia DHT для поиска собеседников по всему миру.

**Подходит для:** обычное общение через интернет.

### 3. Nostr relay (fallback)
Если P2P не работает (NAT, файервол) — сообщение отправляется через публичные Nostr relay-серверы в зашифрованном виде. Relay видит только зашифрованный blob, но не содержимое.

**Подходит для:** сложные сетевые условия, корпоративные сети.

---

## 🚀 Быстрый старт

1. Скачай установщик с [Releases](https://github.com/r2bb1tDev/Soviet/releases/latest)
2. Установи и запусти
3. Введи никнейм — ключи создадутся автоматически
4. Нажми **QR-код / Поделиться** в настройках и скинь приглашение другу
5. Друг вставляет приглашение в **«Добавить контакт»** → отправляется **запрос авторизации**
6. Вы принимаете запрос → только после этого можно писать

---

## 🛠️ Сборка из исходников

```bash
# Требования: Node.js 18+, Rust 1.75+

git clone https://github.com/r2bb1tDev/Soviet.git
cd Soviet
npm install

# Режим разработки
npm run tauri dev

# Продакшен сборка
npm run tauri build
```

**Linux — дополнительные зависимости:**
```bash
sudo apt install libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
```

---

## 🗺️ Roadmap

- [x] v1.0 — LAN-режим, E2E чаты, группы, Nostr каналы
- [x] v1.1 — P2P mesh (libp2p), Nostr DM fallback, поиск пользователей, автообновление
- [x] v1.3 — поиск по переписке, звук ICQ, офлайн store-and-forward, передача файлов
- [x] v1.3.5 — терминальный UI, OS-уведомления, LAN надёжность, фикс билдов
- [x] v2.0 — голосовые сообщения, NAT hole-punching (DCUtR), шифрование БД, ответ на сообщения
- [x] v2.1 — улучшенные каналы: поиск, описание в шапке, копирование ID, терминальный стиль постов
- [x] v2.2 — уведомление об обновлении в трее, кастомные диалоги подтверждения, упрощённый профиль, вставка кода в каналах, авто-обновление реакций/комментариев
- [x] v2.3 — исправлен краш при пересылке поста из канала, снижено свечение интерфейса, синхронизация аватарок, кликабельные ссылки, изменяемая ширина сайдбара, полная светлая тема
- [x] v2.4 — P2P/Nostr для групп и личных чатов, открытие в новом окне, drag & drop файлов, статусы прочитано через все транспорты, пагинация истории, счётчик непрочитанных в трее
- [x] v2.5 — SQLCipher (полное шифрование файла БД), защита от Sybil-атак (репутация P2P-узлов), code signing CI/CD (Windows + macOS)
- [x] v2.5.11 — исправлено белое окно попаута (фон при загрузке), пропажа никнеймов (race condition contacts), утечка popout_registry при закрытии окна
- [x] v2.5.12 — окончательный фикс белого окна: попаут создаётся скрытым, показывается после рендера React (нет белого мигания при любых условиях)
- [x] v2.5.13 — хотфикс: откат `.visible(false)` (ломал открытие окна на Windows); защита от белого мигания теперь полностью на inline `<style>` в `popout.html`
- [x] v2.5.14 — исправлено зависание создания канала: multi-thread tokio для Nostr (команды не блокируются обработкой входящих событий), 5-сек таймаут на подключение к relay, 3-сек таймаут на ответ при создании канала
- [x] v2.5.15 — **починены автоматические обновления:** новая пара minisign-ключей для updater (прошлая была без доступа к секретам в GitHub Actions → `latest.json` не генерировался, клиент всегда получал 404). **⚠️ Одноразовая переустановка:** все пользователи должны скачать v2.5.15 вручную один раз — дальше обновления приходят автоматически
- [x] v2.5.16 — фикс декораций popout (кнопка ✕ + `background_color` на уровне ОС); кнопка 🪟 «Открыть в новом окне» прямо в шапке чата/канала
- [x] v2.5.17 — **архитектурный фикс popout-окон:** popout теперь использует тот же `index.html` что и главное окно (отдельный `popout.html` на части Windows-конфигов WebView2 отдавал чёрный пустой экран — curl возвращал HTML, WebView не рендерил). Метка окна приходит через `initialization_script` → `window.__sovietPopoutLabel`, `main.tsx` рендерит `<PopoutRoot/>` вместо `<App/>`. Глобальные `error`/`unhandledrejection` fallback-рендеры гарантируют что пользователь всегда видит кнопку «Закрыть» — не «застревает» в пустом окне
- [x] v2.5.21 — **критфикс автообновлений:** `latest.json` и `.sig` файлы не генерировались с v2.5.11 (secret подписи имел пароль, который был утрачен; `tauri-action` молча скипал подпись). Новая пара ключей **без пароля** + CI-шаг проверяет наличие secret до сборки. **⚠️ Одноразовая переустановка вручную** из Releases
- [x] v2.5.22 — **Критфикс Nostr DM:** contact requests и входящие Kind 4444 события молча отбрасывались — `on_relay_msg` захватывал пустую identity в момент старта. Теперь identity читается свежей на каждое событие. Popout-окна удалены целиком (заменены вкладками в v2.6). **Друзья за границей наконец могут добавить друг друга через damus.io / nos.lol**
- [x] v2.5.23 — **Linux terminal dark-grey тема** вместо зелёного phosphor: серые оттенки `#1A1A1A / #D0D0D0`, DOS-шрифт сохранён, CRT-scanlines
- [x] v2.5.24 — **ICQ Nudge (Жужжалка)**, **Auto-away** (5 мин idle → «Отошёл»), **экспорт чата в `.txt`**
- [x] v2.5.25 — **Ctrl+F поиск в чате**, **Status presets с кастомным текстом**, **«Последний раз в сети»**, **Stealth mode**, диагностика релизного pipeline (.sig fallback upload в `release.yml` — автообновления наконец чинятся у всех)
- [x] v2.5.26 — **Stealth broadcast-фильтр** (LAN hello не уходит к скрытым контактам), **Away auto-reply** (автоответ при away/dnd), **Умный поиск контактов** (custom_id / pubkey-prefix / nickname), **Цитирование с jump-to-message**
- [x] v2.6.0 — **Multi-chat через вкладки**: одновременно несколько открытых чатов, мгновенное переключение, бейдж непрочитанных на каждой; **inline-превью картинок** (data-URL + image URLs)
- [ ] v2.6.1 — **Multi-window через process-spawn:** настоящие отдельные окна через `std::process::Command` (не Tauri secondary webview, который сломан на Windows WebView2)
- [ ] v2.6.2 — групповые чаты через Nostr-relay (сейчас только LAN/P2P), drag-tab-to-detach, онлайн-timeline контакта за неделю
- [ ] v2.7 — **Multi-device identity (ПК):** экспорт/импорт identity через QR и seed-строку (Argon2id + ChaCha20-Poly1305), один аккаунт на двух ПК, passphrase на старте вместо OS keyring, dedup самого себя в Nostr DM
- [ ] v2.7 — **Адаптивный UI:** медиа-запросы под узкий экран (сайдбар → полноэкранный список, bottom-tabs, swipe-назад), отключение popout/трея на мобильных, подготовка компонентов к touch
- [ ] v2.8 — **Android-сборка:** `cargo tauri android init`, GitHub Actions на `ubuntu-latest`, unsigned APK в Releases, `tauri-plugin-biometric` для пассфразы, Android Foreground Service для фоновой доставки
- [ ] v2.9 — **iOS-сборка (unsigned):** `cargo tauri ios init`, GitHub Actions на `macos-14`, unsigned билд для симулятора (TestFlight/App Store — при появлении Apple Developer $99/год)
- [ ] v3.0 — **Device-to-device history sync:** шифрованный дамп SQLite передаётся между своими устройствами через P2P/LAN при импорте, аккаунт master-key + device sub-keys (NIP-26 delegation для Nostr)
- [ ] v3.1+ — Tor/VPN интеграция, encrypted backup в Nostr, мультиаккаунт, биометрическая разблокировка

---

## 🤝 Контрибьюторы

Проект сделан командой **FSOCIETY × LURKHUB**.
Любые PR и идеи приветствуются — см. [CONTRIBUTING.md](CONTRIBUTING.md).

---

## 📄 Лицензия

MIT — см. [LICENSE](LICENSE). Бесплатно, навсегда, без рекламы.

---

<p align="center">
  Без регистрации · Без слежки · Без серверов · <b>Для людей</b>
</p>
